<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="@android:style/Theme.Material">
        <item name="android:colorPrimary">@color/primColor</item>
        <item name="android:colorPrimaryDark">@color/secColor</item>
        <item name="android:navigationBarColor">@color/secColor</item>
    </style>

    <style name="SplashTheme" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:background">@color/primColor</item>
    </style>

</resources>